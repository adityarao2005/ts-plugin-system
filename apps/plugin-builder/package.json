{
  "name": "plugin-builder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "start": "node dist/index.js",
    "build": "tsc --build",
    "test": "vitest"
  },
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}